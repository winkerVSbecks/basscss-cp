📃BASSCSS CP
  Copies all the compiled CSS files from basscss modules into a directory of your choice. 
  The legacy addons are ignored.

Usage
  npm install basscss basscss-basic basscss-addons
  bash basscss-cp.sh <output_dir_name>
